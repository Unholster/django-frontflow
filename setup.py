#!/usr/bin/env python
from setuptools import setup, find_packages


setup(name="django-frontflow",
      version='0.0.1',
      packages=find_packages(),
      include_package_data=True,
      description='Unholster frontend workflow utilities',
      license='Proprietary',
      install_requires=[],
      )
